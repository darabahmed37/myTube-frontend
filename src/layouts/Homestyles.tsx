import {styled} from "@mui/material";

export const HomeContainer = styled("div")(({theme}) => ({
  background: "#1e1e1e",
  height: "100vh",
  display: "flex",
  minWidth: "1280px",
  alignItems: "center",
}))

